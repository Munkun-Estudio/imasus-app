<%# Reusable 4-card grid for the public resources (Materials, Training,
    Challenges, Glossary). Same styling everywhere it appears — visitor
    home, participant home, facilitator home — so the installation palette
    visual identity stays consistent across roles.

    i18n keys live under home.visitor.resources.* (their original
    location); reused as-is to avoid duplicating copy. %>
<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
  <%= link_to materials_path, data: { resource_card: true, resource: "materials" },
              class: "rounded-2xl border border-brand-primary/10 bg-brand-info/30 p-5 transition hover:bg-brand-info/50" do %>
    <p class="text-xs font-medium uppercase tracking-wide text-brand-primary/50">02</p>
    <h3 class="mt-2 text-base font-bold text-brand-primary"><%= t("home.visitor.resources.materials.title") %></h3>
    <p class="mt-2 text-xs leading-relaxed text-brand-primary/70"><%= t("home.visitor.resources.materials.lead") %></p>
  <% end %>
  <%= link_to training_index_path, data: { resource_card: true, resource: "training" },
              class: "rounded-2xl border border-brand-secondary/10 bg-brand-secondary p-5 text-white transition hover:brightness-110" do %>
    <p class="text-xs font-medium uppercase tracking-wide text-white/60">03</p>
    <h3 class="mt-2 text-base font-bold"><%= t("home.visitor.resources.training.title") %></h3>
    <p class="mt-2 text-xs leading-relaxed text-white/70"><%= t("home.visitor.resources.training.lead") %></p>
  <% end %>
  <%= link_to challenges_path, data: { resource_card: true, resource: "challenges" },
              class: "rounded-2xl border border-brand-primary/10 bg-brand-success/40 p-5 transition hover:bg-brand-success/60" do %>
    <p class="text-xs font-medium uppercase tracking-wide text-brand-primary/50">04</p>
    <h3 class="mt-2 text-base font-bold text-brand-primary"><%= t("home.visitor.resources.challenges.title") %></h3>
    <p class="mt-2 text-xs leading-relaxed text-brand-primary/70"><%= t("home.visitor.resources.challenges.lead") %></p>
  <% end %>
  <%= link_to glossary_terms_path, data: { resource_card: true, resource: "glossary" },
              class: "rounded-2xl border border-brand-primary/10 bg-brand-soft/40 p-5 transition hover:bg-brand-soft/60" do %>
    <p class="text-xs font-medium uppercase tracking-wide text-brand-primary/50">05</p>
    <h3 class="mt-2 text-base font-bold text-brand-primary"><%= t("home.visitor.resources.glossary.title") %></h3>
    <p class="mt-2 text-xs leading-relaxed text-brand-primary/70"><%= t("home.visitor.resources.glossary.lead") %></p>
  <% end %>
</div>
