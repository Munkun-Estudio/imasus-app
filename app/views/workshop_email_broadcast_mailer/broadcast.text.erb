<%= site_short_name %>
<%= @workshop.title %>

<%= @broadcast.body_text %>

<%= t(".footer",
      default: "Sent by %{sender} through %{short_name}.",
      sender: @broadcast.sender.name,
      **site_translation_options) %>
