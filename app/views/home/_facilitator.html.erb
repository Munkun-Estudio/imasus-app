<%# Slice 6 will fill this in: workshops with counts, invite CTA, resources teaser. %>
<div data-home-variant="facilitator" class="px-6 py-12">
  <h1 class="text-2xl font-bold text-imasus-dark-green">
    <%= t(".greeting", name: current_user.name) %>
  </h1>
</div>
