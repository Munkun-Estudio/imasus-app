import { Controller } from "@hotwired/stimulus"

// Locale switching is handled via standard link navigation with ?locale= params.
// This controller exists as a hook for future enhancements (e.g., dropdown).
export default class extends Controller {
}
